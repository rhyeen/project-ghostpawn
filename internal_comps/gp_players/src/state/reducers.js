import * as ActionType from './actions.js';

const INITIAL_STATE = {
  ui: {},
  entities: {}
};

export const gp_players = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
